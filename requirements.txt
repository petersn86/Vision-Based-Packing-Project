## ⚙️ **requirements.txt**

# Core dependencies
opencv-python
tqdm
numpy
ultralytics
ollama

# Configuration management
pyyaml

# Object tracking
filterpy
scipy

# Deep visual re-identification (MobileNetV2 feature extractor)
# If torch/torchvision are unavailable, the tracker falls back
# to an enhanced multi-channel histogram automatically.
torch
torchvision

# QR code detection
pyzbar

# Web interface
flask
flask-cors

# Additional utilities
pillow